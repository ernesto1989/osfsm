const port = 3000;


//URLS 

const indexURL = '/';
const contextURL = '/OSF';
const apiURL = '/api';


const BASE_SCENARIO_ID = 'BASE_CONDITION';

module.exports= {
    port,
    dbHost, 
    dbPort, 
    dbUser, 
    dbPass,
    dbName,
    indexURL,
    contextURL,
    apiURL,
    BASE_SCENARIO_ID
}